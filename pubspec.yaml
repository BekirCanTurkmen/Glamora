name: dolabim
description: "A Flutter project integrated with Firebase Authentication and Firestore."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # iOS gibii ikonlar
  cupertino_icons: ^1.0.8

  # 🔥 Firebase paketleri
  firebase_core: ^3.6.0        # Firebase'i başlatmak için
  firebase_auth: ^5.3.1        # Email/Password giriş sistemi
  cloud_firestore: ^5.4.4      # Firestore veritabanı

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # (İleride kullancağım ) görsel, logo veya json dosyaları için:
  # assets:
  #   - assets/images/
  #   - assets/icons/
